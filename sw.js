// sw.js — минимальный service worker только для того, чтобы Android/Chrome
// считал приложение устанавливаемым (иконка + имя на главном экране).
// Никакого офлайн-кеша сознательно нет: данные приходят из Google Таблиц
// и должны быть всегда свежими, а не подсовываться из кеша.
self.addEventListener('fetch', () => {});
